# uma lista imutável (não dá pra adicionar coisas novas, nem remover e muito menos substituir)
cores = ("vermelho", "verde", "azul")
print(cores)

# 'cores[1]' = amarelo não é possível

# Qualquer um dos os métodos que não alterar a tupla funciona com ela
print(len(cores))
